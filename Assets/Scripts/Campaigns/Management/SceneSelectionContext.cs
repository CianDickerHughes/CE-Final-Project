using UnityEngine;

//This class will help us manage the context for what scene we are loading within a campaign
//FOR NOW
//We could implement proper/other level loading techniques later on if that works better but for now we will keep it simple

public class SceneSelectionContext : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
